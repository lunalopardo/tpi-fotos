doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Fotaza 2
    link(rel="stylesheet", href="/styles.css")
  body
    nav.navbar
      div.navbar-brand FOTAZA 2
      ul.navbar-menu
        li
          a.nav-link(href="/" class=(currentPath === '/' ? 'active' : '')) Inicio
        li
          a.nav-link(href="/auth/login" class=(currentPath === '/auth/login' ? 'active' : '')) Iniciar Sesión

    main.container
      block content